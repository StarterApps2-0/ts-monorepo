{
  "name": "shared",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "source": "./src/index.ts",
  "private": true,
  "files": [
    "dist/**"
  ],
  "scripts": {
    "test": "jest",
    "dev": "tsc -w",
    "build": "tsc",
    "lint": "TIMING=1 eslint src --fix",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "eslint": "^7.32.0",
    "jest": "^26.6.3",
    "scripts": "*",
    "tsconfig": "*",
    "typescript": "^4.5.3"
  },
  "dependencies": {
    "config": "*",
    "express-validator": "^6.14.0",
    "knex": "^0.95.15",
    "objection": "^3.0.1",
    "pg": "^8.7.1",
    "pretty-error": "^4.0.0",
    "tsconfig": "*",
    "winston": "^3.3.3"
  }
}
